const entree = "Enchiladas";
console.log(entree); // outputs Enchiladas

/* a const variable cannot be reassigned because it is constant. 
If you try to reassign a const variable, you’ll get a TypeError. */
